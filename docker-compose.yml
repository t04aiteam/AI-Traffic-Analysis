version: "3.8"
services:
  app:
    build: .
    ports:
      - "7863:7863"         # host:container
    volumes:
      - .:/app
    working_dir: /app
    command: uvicorn webapp.backend.main:app --host 0.0.0.0 --port 7863 --reload --reload-dir /app/webapp
